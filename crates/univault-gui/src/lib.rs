//! Reusable UI pieces of the univault GUI, exposed as a library so
//! the app binary and the component preview harness
//! (`cargo run -p univault-gui --bin preview -- <component>`) share
//! one implementation.

pub mod components;
